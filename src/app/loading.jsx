import React from 'react';

const loading = () => {
    return (
        <div>
            <span className="loading loading-spinner loading-lg flex mx-auto py-20"></span>
        </div>
    );
};

export default loading;